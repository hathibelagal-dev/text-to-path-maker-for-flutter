name: icon_animation
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  text_to_path_maker: ^0.5.1

flutter:
  uses-material-design: true
  assets:
    - assets/
